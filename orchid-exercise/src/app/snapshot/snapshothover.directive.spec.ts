import { SnapshothoverDirective } from './snapshothover.directive';

describe('SnapshothoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SnapshothoverDirective();
    expect(directive).toBeTruthy();
  });
});
